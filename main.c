#include "main.h"
#include <stdlib.h>
#include <time.h>

struct CardPile drawPile;
struct CardPile tableau[7];
Card foundation[4];

struct Selection selection;

int main() {
  srand(time(NULL));
  return 1;
}
